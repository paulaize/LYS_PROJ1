[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["data", "work", "outputs", ".git", ".codex-log", "legacy/context_code"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = []
